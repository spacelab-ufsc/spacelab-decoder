************
Installation
************

Before using the SpaceLab Decoder, ensure that you have the necessary dependencies installed, including Python 3, GTK 3, and the required Python libraries (e.g., `numpy`, `scipy`, `pyngham`).
